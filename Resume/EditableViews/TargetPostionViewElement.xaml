<UserControl x:Class="Resume.EditableViews.TargetPostionViewElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Resume.EditableViews"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="Должность"/>
        <TextBox Text="{Binding Path=Position, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Интересы"/>
        <DataGrid ItemsSource="{Binding Path=Reasons, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" CanUserDeleteRows="True" CanUserAddRows="True" 
                  HeadersVisibility="None"/>
    </StackPanel>
</UserControl>
